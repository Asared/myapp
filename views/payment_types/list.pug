extends ../layout

block content
    h1= title
    table
        tr 
            th id
            th Имя
        each item in payment_types
            tr
                td= item.id
                td= item.label